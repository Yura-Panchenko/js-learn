import React from "react";
import s from './style.module.css';

const Header = () => {
    return (
        <header className={s.header}>
            Header
        </header>
    )
}

export default Header;